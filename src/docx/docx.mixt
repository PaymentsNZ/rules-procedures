namespace docx

meta {
}

{
	// docx::class is used to provide a class name to a DOCX element
	docx::class = (className as text, content as any ) => (
		content <@ {
			docx::class = className
		}
	)
}